<div class="centered">
    <h1>{{title}}</h1>
    <hr>
    <div>
        <img src="{{imageUrl}}" alt="{{title}}">
    </div>
    <h2>{{price}}</h2>
    <p>{{description}}</p>
    {{> add-to-cart}}
</div>