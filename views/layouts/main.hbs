<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css">
    {{#if productsCSS}}
    <link rel="stylesheet" href="/css/product.css">{{/if}}
    {{#if formsCSS}}
    <link rel="stylesheet" href="/css/forms.css">{{/if}}
    {{#if ordersCSS}}
    <link rel="stylesheet" href="/css/orders.css">{{/if}}
    {{#if cartCSS}}
    <link rel="stylesheet" href="/css/cart.css">{{/if}}
    {{#if authCSS}}
    <link rel="stylesheet" href="/css/auth.css">{{/if}}
    <title>Shopy</title>
</head>

<body>
    <header class="main-header">
        <nav class="main-header__nav">
            <ul class="main-header__item-list">
                <li class="main-header__item">
                    <a class="{{#if activeIndex }}active{{/if}}" href="/">Shop</a>
                </li>
                <li class="main-header__item">
                    <a class="{{#if activeProducts }}active{{/if}}" href="/products">Products</a>
                </li>
                {{#if isAuthenticated}}
                <li class="main-header__item">
                    <a class="{{#if activeCart }}active{{/if}}" href="/cart">Cart</a>
                </li>
                <li class="main-header__item">
                    <a class="{{#if activeOrders }}active{{/if}}" href="/orders">Orders</a>
                </li>
                <li class="main-header__item">
                    <a class="{{#if activeAddProduct }}active{{/if}}" href="/admin/add-product">Add product</a>
                </li>
                <li class="main-header__item">
                    <a class="{{#if activeAdminProducts }}active{{/if}}" href="/admin/products">Admin Products</a>
                </li>
                {{/if}}
            </ul>
            <ul class="main-header__item-list">
                {{#if isAuthenticated}}
                <li class="main-header__item">
                    <form action="/logout" method="POST">
                        <input type="hidden" name="_csrf" value="{{csrfToken}}">
                        <button type="submit">Logout</button>
                    </form>
                </li>
                {{else}}
                <li class="main-header__item">
                    <a class="{{#if activeLogin }}active{{/if}}" href="/login">Login</a>
                </li>
                <li class="main-header__item">
                    <a class="{{#if activeSignup }}active{{/if}}" href="/signup">Signup</a>
                </li>
                {{/if}}
            </ul>
        </nav>
    </header>

    <main>
        {{{body}}}
    </main>

</body>

</html>